# Compiled Go binaries
dpls-scheduler
dpls-scheduler.exe
*.exe

# Compiled eBPF object files (generated from tc_bridge.c via clang on Linux)
# Do NOT commit — regenerate with: clang -target bpf -O2 ... tc_bridge.c -o tc_bridge.o
*.o

# bpf2go generated files (regenerate with: go generate ./internal/ebpf/)
# Uncomment below if you want to exclude generated Go bindings too:
# internal/ebpf/tc_bpf*.go

# Temp/scratch files from PDF analysis
dpls_page_*.txt
btp_text.md
btp_extracted.txt

# IEEE papers — copyrighted, cannot be redistributed publicly
# Store these locally only — do NOT commit to public repos
Dependency-Aware_*.pdf
Dynamic_Caching_*.pdf

# OS junk
.DS_Store
Thumbs.db

# Ignore all markdown files (like claude.md, gemini.md) EXCEPT the main experiments result
*.md
!comprehensive_experiments.md
